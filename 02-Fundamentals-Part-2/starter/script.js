"use strict"

/*
const temperatures = [3,-2,-6,-1,'error', 9,13,17,15,14,9,5]
const calcTempAmplitude = function (temp,temp2){
    let arr = temp.concat(temp2)
let max1 = arr[0]
let min1= arr[0]
    for(let i=0;i<arr.length;i++){
        let currentValue = arr[i]
        if( typeof currentValue =="number") {
            max1 < arr[i] ? max1 = arr[i] : ''
            min1 > arr[i] ? min1 = arr[i] : ''
        }
    }
    console.log(max1,min1)

  return max1-min1
}
calcTempAmplitude(temperatures,[100,20,5,-20])*/

/*
const bills =[22, 295, 176, 440, 37, 105, 10, 1100, 86 , 52]
const tips =[]
const totals =[]
const calcTip = function (bill) {
    return bill >= 50 && bill <= 300 ? bill * 0.15 : bill * 0.2;
}

for(let i=0;i<bills.length;i++){
   let chai = calcTip(bills[i])
    totals.push(bills[i] + chai)
    tips.push(chai)
}
console.log(tips)
console.log(totals)

const calcAverage = (arr) => {
    let length = arr.length
    console.log((arr.reduce((a,b)=>a+b))/length)
}
calcAverage([5,5,5,5,1000])
*/


